本次提交包含以下核心功能：

修复 UI 布局异常：解决了个人中心“关注”和“粉丝”列表错误居中的问题，恢复了正常的左右对齐布局。
修复数据逻辑错误：
前端：修正了“我的收藏”数据解析逻辑，使其能正确读取后端返回的包装对象格式。
后端：修正了“我的赞”获取逻辑，确保返回的是“我点出的赞”而非“我收到的赞”。
修复 500 报错：解决了点击“取消关注”时的 500 服务器错误，通过在后端添加事务管理（@Transactional）确保了数据库删除操作的安全性。
优化代码结构：将控制器（Controller）中的业务逻辑剥离至服务层（Service），并清理了前端冗余的初始化代码。
